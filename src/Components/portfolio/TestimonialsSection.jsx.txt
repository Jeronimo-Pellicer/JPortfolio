import React from 'react';
import { motion } from 'framer-motion';
import { Quote, Star } from 'lucide-react';
import { useLanguage } from './LanguageContext';

const testimonials = [
    {
        name: 'María González',
        role: 'CEO, Estudio Notarial Castro',
        image: 'https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=400&h=400&fit=crop',
        content: {
            en: "Jerónimo transformed our customer service system completely. His data-driven approach reduced response times by 40% and significantly improved client satisfaction. He's a true professional.",
            es: "Jerónimo transformó completamente nuestro sistema de atención al cliente. Su enfoque basado en datos redujo los tiempos de respuesta en un 40% y mejoró significativamente la satisfacción del cliente. Es un verdadero profesional."
        },
        rating: 5
    },
    {
        name: 'Carlos Mendoza',
        role: 'Marketing Director, TechStart',
        image: 'https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=400&h=400&fit=crop',
        content: {
            en: "Working with Jerónimo on our digital marketing strategy was exceptional. His SEO expertise and content strategy helped us triple our organic traffic in just 6 months.",
            es: "Trabajar con Jerónimo en nuestra estrategia de marketing digital fue excepcional. Su experiencia en SEO y estrategia de contenido nos ayudó a triplicar nuestro tráfico orgánico en solo 6 meses."
        },
        rating: 5
    },
    {
        name: 'Laura Fernández',
        role: 'Operations Manager, Digital Solutions',
        image: 'https://images.unsplash.com/photo-1438761681033-6461ffad8d80?w=400&h=400&fit=crop',
        content: {
            en: "Jerónimo's analytical skills and empathetic approach to customer experience made him invaluable to our team. His process optimizations saved us countless hours every week.",
            es: "Las habilidades analíticas de Jerónimo y su enfoque empático hacia la experiencia del cliente lo hicieron invaluable para nuestro equipo. Sus optimizaciones de procesos nos ahorraron incontables horas cada semana."
        },
        rating: 5
    }
];

export default function TestimonialsSection() {
    const { language, t } = useLanguage();

    return (
        <section className="py-20 sm:py-32 bg-zinc-900 relative overflow-hidden">
            {/* Background effects */}
            <div className="absolute inset-0">
                <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-emerald-500/5 rounded-full blur-3xl" />
            </div>

            <div className="container mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
                <div className="max-w-7xl mx-auto">
                    {/* Section header */}
                    <motion.div
                        initial={{ opacity: 0, y: 20 }}
                        whileInView={{ opacity: 1, y: 0 }}
                        viewport={{ once: true }}
                        transition={{ duration: 0.6 }}
                        className="text-center mb-12 sm:mb-16"
                    >
                        <span className="text-emerald-400 font-medium tracking-wide text-sm uppercase">
                            {language === 'en' ? 'Testimonials' : 'Testimonios'}
                        </span>
                        <h2 className="text-4xl md:text-6xl font-bold text-white mt-4 mb-6">
                            {language === 'en' ? 'What Clients Say' : 'Lo Que Dicen Los Clientes'}
                        </h2>
                        <p className="text-zinc-400 max-w-2xl mx-auto text-lg">
                            {language === 'en' 
                                ? 'Real feedback from professionals I\'ve had the pleasure to work with.' 
                                : 'Comentarios reales de profesionales con los que he tenido el placer de trabajar.'}
                        </p>
                        <motion.div 
                            initial={{ width: 0 }}
                            whileInView={{ width: 80 }}
                            viewport={{ once: true }}
                            transition={{ delay: 0.2, duration: 0.8 }}
                            className="h-1 bg-gradient-to-r from-emerald-500 to-teal-500 mx-auto rounded-full mt-6" 
                        />
                    </motion.div>

                    {/* Testimonials grid */}
                    <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
                        {testimonials.map((testimonial, index) => (
                            <motion.div
                                key={testimonial.name}
                                initial={{ opacity: 0, y: 60, scale: 0.9 }}
                                whileInView={{ opacity: 1, y: 0, scale: 1 }}
                                viewport={{ once: true, margin: "-100px" }}
                                transition={{ 
                                    duration: 0.6, 
                                    delay: index * 0.15,
                                    ease: [0.6, 0.05, 0.01, 0.9]
                                }}
                                whileHover={{ y: -8, scale: 1.02 }}
                                className="bg-zinc-800/40 backdrop-blur-xl border border-zinc-700/50 rounded-3xl p-8 hover:border-emerald-500/50 transition-all duration-300 hover:shadow-[0_20px_60px_rgba(52,211,153,0.2)] cursor-pointer relative"
                            >
                                {/* Quote icon */}
                                <div className="absolute top-6 right-6 opacity-10">
                                    <Quote className="w-16 h-16 text-emerald-400" />
                                </div>

                                {/* Rating */}
                                <div className="flex gap-1 mb-4">
                                    {[...Array(testimonial.rating)].map((_, i) => (
                                        <Star key={i} className="w-5 h-5 fill-emerald-400 text-emerald-400" />
                                    ))}
                                </div>

                                {/* Content */}
                                <p className="text-zinc-300 leading-relaxed mb-6 relative z-10">
                                    "{testimonial.content[language]}"
                                </p>

                                {/* Author */}
                                <div className="flex items-center gap-4 pt-6 border-t border-zinc-700/50">
                                    <motion.img
                                        whileHover={{ scale: 1.1 }}
                                        src={testimonial.image}
                                        alt={testimonial.name}
                                        className="w-12 h-12 rounded-full object-cover ring-2 ring-emerald-500/20"
                                    />
                                    <div>
                                        <p className="text-white font-semibold">{testimonial.name}</p>
                                        <p className="text-zinc-500 text-sm">{testimonial.role}</p>
                                    </div>
                                </div>
                            </motion.div>
                        ))}
                    </div>
                </div>
            </div>
        </section>
    );
}